<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ui.activities.loading.LoadingActivity">

    <TextView
        android:id="@+id/tvViewResponse"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:textAlignment="center"
        tools:text="@string/text_wait_validation"
        android:textSize="16sp"
        android:layout_marginHorizontal="@dimen/full_margin"
        android:layout_marginVertical="@dimen/double_margin"
        />

    <ImageView
        android:id="@+id/ivViewResponse"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tvViewResponse"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_margin="@dimen/full_margin"
        tools:src="@drawable/ic_monitor"
        android:contentDescription="@string/text_image_status"
        />


</androidx.constraintlayout.widget.ConstraintLayout>